# 選配相依：只有「活用」題需要，用來切詞並判斷五段／一段
# （純字典查表分不出「切る」與「見る」，猜錯會生出「見らない」這種不存在的詞）。
#
# scripts/build_quiz.py 對這兩個套件是 try-import。沒安裝時只是不出活用題，
# 語彙、文法、用法題照常生成，生成器本身仍不依賴任何外部套件。
#
# 釘住版本：詞典有 248MB，且這是唯一會被拉進 CI 的第三方程式碼。
fugashi==1.5.2
unidic-lite==1.0.8
