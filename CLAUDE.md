# CLAUDE.md

這份文件給 Claude Code 讀。開啟這個專案時請先看完，再開始動作。

---

## 這是什麼

一個把 Notion 筆記自動轉成日文測驗網站的專案。

使用者（Sean）在 Notion 用 Bite Size Japanese podcast 學日文（N3–N2 程度），
長期累積兩類筆記。這個專案把筆記抓下來、依規則生成 JLPT 風格的選擇題，
用 GitHub Actions 每天重建，部署到 GitHub Pages。

**現況：已實跑驗證並部署上線**（https://pandemonium0225.github.io/jlpt-quiz/ ，
repo：`pandemonium0225/jlpt-quiz`，public，因為免費方案的 private repo 不能用 Pages）。
parser 已對照實際 Notion 結構修正過；之後改規則前，先看下面「出題規則」與「待辦」的驗證紀錄。

## 資料來源

兩個 Notion 頁面，ID 已固定：

| 用途 | Page ID |
|---|---|
| 日文文章（父頁面，底下約 27 篇子頁面） | `feec6a8276b64996a983402ca73738c4` |
| 日文文法（上面那頁的子頁面之一） | `3ce0db2643d081c69f7fdb6e2e41f2b9` |

### 日文文章：逐字稿 + 使用者的手動標記

每篇是一集 podcast 的逐字稿整理，格式大致是「日文原句（漢字附平假名）」
後面接一個 `📝` 開頭的 quote block 放中文翻譯。

關鍵：**使用者會手動把生詞標成粗體或加背景色**。這些標記是語彙題的唯一來源。

已知的格式不一致，parser 必須容忍：
- 時間戳有時是行內粗體 `**[0:43]**`，有時是獨立的 heading `### 0:00`
- 行內粗體的時間戳會被誤判成標記詞，已用 `TIMESTAMP` regex 排除
- 使用者的標記有兩種性質，只有第一種適合出題：
  - 詞彙：ボーダーコリー、なまけて、しょうもない、中絶、申請、親しい
  - 文法碎片：「は」「きゃ」「のって」「方」「ずつ」
  - 目前用長度與純假名規則過濾（見 `marked_terms()`），已對上述實例驗證過

### 日文文法：結構化的文法筆記

每則的結構固定：

```
## 【文法點名稱】
#### 📅 日期
### 問題解析
（散文說明）
### 接續方式
（表格：接続 | 例）
### 例句
日文例句
> 📝 中文翻譯
```

對應到 Notion API 的 block type（**實測結果**）：文法點標題是 `heading_2`，
區塊標題（問題解析／接續方式／例句）是 `heading_3`，日期是 `heading_4`，
表格是 `table` + `table_row` 子 block。parser 以「標題文字以【開頭」判斷文法點，
不依賴 heading 層級。例句沒有任何粗體或底色標記。

目前約有 20 則文法點。

## 出題規則

### 2026-09-19 更新（目前實作，以此段與 README 為準）

使用者要求改善難讀的題目、標點與依 Notion 內容動態出題，並確認保留每天同步及手動重建。

- 現在定位為「依筆記原句複習」，不宣稱其他選項在所有日文語境下都錯誤；仍是規則式生成，沒有 LLM。
- 語彙遞迴讀取文章子頁面與巢狀段落／清單／toggle／callout。使用者後續明確要求缺標點應自動補齊：先依空白、換行、明確句尾與接續語補標點，再切句，不再排除未斷句逐字稿。保護引號／讀音／標記詞，原始段落另存 `source_text` 供作答後對照，不回寫 Notion。仍排除詞條＋中譯、時間戳、括號破損或挖空後無語境的題目；片語沒有固定長度上限。
- 語彙選項依表面詞形、去讀音後的長度篩選，優先同篇來源；不足三個誘答就略過，不用所有詞混湊。
- 文法優先用例句的粗體／上色標記，未標記才由標題推導；短助詞需接續表例子的相鄰文字佐證。無法分類的文法不再互當誘答；常見可互換助詞亦不互當誘答。
- 接續題用每一列的具體例子詢問對應接續，避免將對照／別用法誤當標題文法的正解；不限前兩列，只讀接續區表格。
- 題庫 schema_version 為 2；題幹是純文字，指示、原句、中譯、來源與解說分欄。前端先跳脫 HTML，再產生 ruby 注音；填空統一為 `［　］`。翻譯及來源只在計分後呈現。
- 全量重建反映新增／修改／刪除，去重並移除同題幹的答案衝突；寫完整的暫存 JSON 後才原子替換。每輪平衡抽取題型，最多十題，同一原句不重複。
- 檢查：`python -m unittest discover -s tests -v`、`node --test tests/test_ui.cjs`；CI 先測試再抓 Notion。Python 生成器仍無外部相依。

### 2026-09-21 更新（前端改版，與上一段並存）

使用者要求「日式字體、簡約、排版舒服大氣」，並在過程中指出接續題缺脈絡。

- 視覺：Zen Kaku Gothic New（日文）＋ Noto Sans TC（中文）。色票只有紙 `#f5f4f0`、
  墨 `#1f1d1b`、薄墨、罫線，加上**唯一的彩色**朱 `#d7182a`。朱色只用在正解那一列
  （左緣紅線＋○），答錯用墨色 ×，不用綠／紅對比 —— ○× 是日本考卷的語彙，
  而且不靠顏色分辨對錯，色盲也讀得出來。改動前刻意避開了幾個通用生成式版面的特徵：
  中點串接的 meta 字串、按鈕文字後掛箭頭、帶色近黑代替黑、裝飾用的細進度條。
- 流程改成**一次一題**：選完立刻判定並展開解說，按「下一題」前進，第十題後是結果頁。
  頁面最上方的答案欄（十格 ○／×／未答）同時是進度、成績與回看入口，取代了進度條與大字分數。
- 題幹區固定保留兩行高度（`min-height:5em`），單行題目下方會留白，換取連續作答時選項不跳動。
- 鍵盤：`1`–`4` 選答，`Enter` 或 `→` 下一題。
- 接續題在作答時顯示所屬文法點（`q.source`）。這不只是補脈絡：沒有文法點限定，
  同一個例子可以對應多個接續描述，答案不唯一 —— 與當初否決「這句用了哪個文法」
  辨識題是同一個毛病。**文法題不可以比照辦理**，那裡的標題就是答案來源。
- 解說區不再重複顯示「文法點：X」與「來源 X」；每題相同的但書降成小字。
- `tests/test_ui.cjs` 只多了 `document.addEventListener` 的 mock，五個既有斷言未放寬。

### 2026-09-21 更新（接續題改成用法辨識）

原本的接續題問「這個例子對應接續表的哪一列」。兩個毛病：考的是記不記得筆記的**措辭**
而不是日文；同一個例子常符合多個接續描述，答案不唯一（與當初否決「這句用了哪個文法」
辨識題是同一件事）。改成問「這個助詞在這句表示什麼」。

- `usage_role()` 從接續欄抽語意角色。括號標籤優先（`名詞＋と＋名詞（全部列挙）` → 全部列挙），
  沒有才取第一段（`場所＋に＋ある／いる` → 場所）。三種情況回 None：詞類描述
  （`な形容詞語幹＋で`）、以平假名結尾的句型片段（`場所に＋名詞＋が`）、括號標了
  対比／別用法的列。同一則裡角色撞名的整組排除，因為分不出來。
- 誘答優先取**同一個助詞的其他用法**，那才是真的會混淆的；不足三個才向同 `category`
  的文法點借。外借時排除兩種：與正解互相包含的（起点／起点・書面語），以及
  **文法點標題已經標明的用法** ——【格助詞　から（起点・口語）】底下的「9時から始まる」
  確實是起点，不能拿「起点」當錯誤選項。
- `kind` 仍是 `connect`（前端與抽題平衡不變），`label` 改為「用法」。
  `add()` 多了 `ask` 參數覆寫題目指示。
- 實際產出 26 題，全部來自格助詞。非格助詞的文法點沒有 `category`，借不到誘答，
  目前出不了題；只有一列的（【格助詞　へ】）也出不了。
- 前端：用法題的 `original` 等於 `stem`、`source` 已顯示在題目上方，解說不再重複。
- 抽不出語意角色的 14 列是**活用形描述**（`動詞辞書形＋名詞`、`可能形＋ように`），
  那是下一階段的活用挖空題，需要斷詞器，見下。

### 斷詞器（已驗證可行，尚未整合）

活用挖空題要做兩件純 stdlib 做不到的事：從例句切出該挖的詞、生出同一個詞的其他活用形。
卡點是**る 結尾動詞分不出五段或一段**（切る／見る），沒有詞典就會生出「見らない」這種錯字。

實測 `fugashi` + `unidic-lite` 可解，24 個案例全對：

- 用 `orthBase` 而非 `lemma`。lemma 會正規化（帰る → **返る**，漢字就錯了），
  也會把可能形「話せる」還原成「話す」，配上「下一段」的活用型會生出「話て」。
- `cType` 直接給活用型（五段-ワア行／下一段-ラ行／サ行変格），不必猜。
- 名詞＋する 要合併成複合動詞，否則「勉強すれば」會掉成「する」。
- 驗證生成結果時要放進上下文（`使います` 而非 `使い`），連用形單獨解析會被判成名詞。
- 詞典 **248MB**（不是我一開始說的 50MB），走 mmap 所以載入是 0.00s。
  只影響生成端，不進 repo 也不進網站。打算做成選配 try-import，沒裝就跳過活用題。

### 2026-09-21 更新（活用題，斷詞器已整合）

接續表裡描述活用形而非語意的列，改出 JLPT 式的挖空題。共 9 題，分兩種來源：

- **例欄已寫成「原形 → 變化形」**（`静かだ → 静かで`）：4 題，**不需要斷詞器**。
  語幹＝原形與變化形的共同前綴；誘答是把同一則其他列的語尾套錯在這個詞上
  （静か＋くて、静か＋て），再加上原形本身（忘了變形）。都是學習者真的會犯的錯。
- **要從例句切出動詞**（`よく使う言葉` → 挖「使う」）：5 題，需要斷詞器。
  `conjugation_target()` 用接續欄第二段定位：字面就比對字面（＋と、＋ように），
  詞類就查詞性（＋名詞）。命中不是剛好一個就放棄。

規則：

- **誘答排除同一則其他列宣告的形。**【動詞辞書形＋名詞】底下也列了た形，
  等於說「よく使った言葉」是對的，不能當錯誤選項。這條同時自動修好
  【〜ように】那兩題（同則列了可能形與ない形，所以「話せない」「忘れる」也排除）。
  與用法題「標題已宣告的用法不當誘答」是同一個原則。
- **活用題不在作答時顯示文法點標題**，因為標題本身就是答案
  （【動詞辞書形＋名詞】、【ないといけない】）。用法題相反，必須顯示才知道在問哪個助詞。
  前端以 `q.label === '用法'` 區分，不是 `q.kind`。
- 注音靠 `furigana_prefix()` 跟著語幹走：「使（つか）う」→「使（つか）って」。

斷詞器（`fugashi` + `unidic-lite`，釘在 `requirements.txt`）：

- `build_quiz.py` 是 try-import，沒裝只是不出需要切詞的那 5 題，其餘照常。
  測試用 `@unittest.skipUnless(b.TAGGER, ...)` 標記，未裝時跳過 4 項。
- CI 已加 `pip install -r requirements.txt` 與 `cache: 'pip'`
  （cache 需要依賴檔，所以 `requirements.txt` 是必要的，不只是為了釘版本）。
- 本機用 `.venv`（已加入 `.gitignore`）：`.venv/bin/python scripts/build_quiz.py`。

### 語意歧義的誘答（2026-09-21，使用者確認後捨棄）

活用題曾出現四個選項都是合法日文、只有語意不同的情況：

```
よく［　］言葉        使う ✓  但「よく使わない言葉」也對
送って［　］といけない   もらわない ✓  但「送ってもらうといけない」也對
［　］ようにメモする    忘れない ✓  但「忘れたようにメモする」＝裝作忘了，也對
［　］ようになる      話せる ✓  但「話せたようになる」使用者判定不自然，一併捨棄
```

活用題刻意不顯示文法點（標題就是答案），學習者沒有線索排除。寫成兩條一般規則，
不寫死題目；誘答因此不足三個的題目由 `add()` 自動捨棄。

- **`OPPOSITE`：辞書形與ない形不互當誘答。** 與既有的「常見可互換助詞不互當誘答」
  是同一個原則。
- **`PLAIN_FORM_TAILS`：接「ように」時，普通形（辞書形／ない形／た形）彼此不當誘答。**
  上一條管不到這個，因為問題出在た形，不是肯定否定對立。

**第一版我把第一條寫成「ない形 vs 其他所有形」**，正解是ない形時連帶殺光誘答，
題目整個消失（9 → 5 題），還誤殺了一題乾淨的。使用者指出應該只排除對立的那一個。
改正後 8 題，加上「ように」那條成為 6 題。**對立關係是成對的，不是單向的。**

活用題最終 6 題：4 題配對格式（不需斷詞器）＋ 2 題切詞
（`送って［　］といけない`、`昨日［　］言葉`）。
**斷詞器目前只換到那 2 題**，投報率要如實記著；整合是選配且測試完整，
筆記增加時價值會回升。

### 待回報的既有問題（不是這次改的，未動）

文法題的「を」誘答池含「から」：`電車（でんしゃ）［　］降（お）ります。` 正解「を」，
但「電車から降ります」也是合法日文。`alternatives` 只涵蓋 `に／へ`、`と／や`、
`から／より`、`が／の`，沒有涵蓋「を／から」的起点用法重疊。要改需先與使用者確認。

以下保留的是先前規則與驗證紀錄；與上述更新衝突時，不要將舊行為改回來。

| kind | 題型 | 來源 | 作法 |
|---|---|---|---|
| `vocab` | 語彙 | 文章的標記詞 | 從原句挖空，誘答取自其他標記詞（挑字數相近的） |
| `grammar` | 文法形式の判断（挖空填入） | 文法頁的例句 | 從標題推出關鍵字，在例句中挖空；誘答取同類別的其他關鍵字 |
| `connect` | 接續 | 文法頁的接續表格 | 給文法點，問正確接續形式，誘答是**其他文法點**的接續 |

已與使用者確認的規則（不要擅自改回去）：

- **語彙**：片語也算生詞（不做長度過濾）。排除：時間戳（含 `00:00:00 –> 00:00:35` 區間）、
  含阿拉伯數字的詞（金額、年齡、數量）、整行都被標記而挖空後沒有語境的題目。
- **文法**：不用「這句用了哪個文法」的辨識題（不是 JLPT 題型，也會有多個正解）。
  關鍵字在句中必須「恰好出現一次」才挖空，比對時先遮掉注音；挖空後句子相同但正解不同
  （と／や、から／より）的整組捨棄；誘答只取實際成功挖過空的關鍵字、且同類別
  （格助詞對格助詞，沒有類別的文法點互為誘答）。標題會去掉注音括號，並截掉「：」之後的說明。
- **接續**：誘答排除同一文法點的其他接續（它們也是正解）。
- 已知限制：仍有語意歧義的題（如 に／へ），「動詞辞書形＋名詞」與「〜で（中止形）」挖不出空。
  若要根治，需使用者在 Notion 例句裡把文法重點標粗體，再改成讀取標記。

生成的是「題庫」不是「考卷」：`quiz.json` 存所有題目與誘答候選池，
前端每次載入隨機抽 10 題、隨機排列選項。使用者按「換一組題目」可重抽，
不需要重新部署。

## 檔案結構

```
scripts/build_quiz.py      抓 Notion → 產生 site/quiz.json（純 stdlib，無相依套件）
site/index.html            測驗 UI，fetch quiz.json
site/quiz.json             生成產物（被 .gitignore 排除，由 CI 每次重建；本機可自己跑產生）
.env / .env.example        本機環境變數（.env 被排除；.env.example 是範本）
.github/workflows/deploy.yml   每天 UTC 23:00 重建並部署
README.md                  給使用者看的設定步驟
```

## 部署流程紀錄（1–3 已全部完成，重做或排錯時參考）

### 1. 環境檢查

確認 `gh auth status` 已登入。若未登入，告訴使用者要跑什麼，停下來等他。

### 2. 實跑驗證（最重要，不要跳過）

```bash
export ARTICLES_PAGE_ID=feec6a8276b64996a983402ca73738c4
export GRAMMAR_PAGE_ID=3ce0db2643d081c69f7fdb6e2e41f2b9
export NOTION_TOKEN='...'    # 停下來讓使用者自己輸入
python3 scripts/build_quiz.py
```

跑完打開 `site/quiz.json`，向使用者回報：

- 三種 kind 各幾題
- 各抽兩題印出完整內容（題幹、正解、誘答池前幾個）
- 有沒有明顯壞掉的題目：題幹被句子切割切壞、誘答一眼可排除、
  挖空後句子讀不通、標記詞含多餘的活用詞尾

**發現問題時先跟使用者討論再改規則，不要自行決定什麼叫「合理的題目」。**
判斷題目品質需要日文語感，使用者才是能判斷的人。

常見失敗與處理方向：
- API 回 404 → integration 沒被加入 Notion 頁面，請使用者到頁面右上 `⋯` → 連線 加入
- 文法題數為 0 → heading level 判斷錯了，Notion API 的 heading_1/2/3 與
  markdown 的 `#` 數量不一定對應，印出實際 block type 確認
- 語彙題數過少 → 過濾規則太嚴，或只有少數文章有標記（後者是正常的，
  使用者才剛開始標記，告訴他多標幾篇就會變好）

### 3. 建 repo 並部署

```bash
git init && git add -A && git commit -m "Notion JLPT quiz generator"
gh repo create jlpt-quiz --private --source=. --push

gh secret set NOTION_TOKEN          # 互動式輸入，不要寫在指令列
gh variable set ARTICLES_PAGE_ID --body feec6a8276b64996a983402ca73738c4
gh variable set GRAMMAR_PAGE_ID --body 3ce0db2643d081c69f7fdb6e2e41f2b9

gh api -X POST repos/{owner}/jlpt-quiz/pages -f build_type=workflow   # 409 = 已啟用，可忽略
gh workflow run "Build and deploy quiz"
gh run watch
```

完成後把網站網址給使用者。

實際執行時的差異：免費方案的 private repo 啟用 Pages 會回 422，所以 repo 改成了 public
（已掃描確認歷史紀錄沒有 token）；`E:` 槽不記錄檔案擁有者，git 需要
`safe.directory`（已加入全域設定）。Actions 每次 push 到 main 也會觸發重建。

## 規範

- **Token 絕對不進 commit、不進指令列參數。** 本機開發只能放在被 `.gitignore` 排除的
  `.env`（由使用者自己建立並貼上，Claude 不要讀取或列印這個檔案）；CI 與 `gh secret set` 維持互動式輸入。
- 建 `.gitignore` 排除 `__pycache__/`、`.env`、`*.token`
- `build_quiz.py` 維持零外部相依（只用 stdlib），這樣 CI 不需要裝套件、也不會有供應鏈風險
- 使用者用繁體中文溝通；程式碼註解也用繁體中文
- 使用者偏好 Python
- 「行」指 columns、「列」指 rows（台灣用法），討論資料結構時注意

## 未來可能的調整方向

使用者提過但尚未決定的：

- 錯題本：記錄答錯的題目，下次優先出（需要 localStorage 或後端）
- 依文章來源篩選題目（只練某一集）
- 文法題可以再加「文の組み立て（★）」之類更難的題型

這些都還沒討論細節，不要自己先做。
